import 'package:flutter/material.dart';

class PageModule extends StatelessWidget {
  const PageModule({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}